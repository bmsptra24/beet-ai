import { ReactNode } from "react";

export type childrenProps = {
  children: ReactNode;
  color: string;
};

export type textProps = {
  text: string;
};

export type pathProps = {
  path: string;
};
