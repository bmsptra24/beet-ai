// import { NextRequest, NextResponse } from "next/server";

// export const authentication = (req: NextRequest) => {
//   const apiKey = process.env.NEXT_API_KEY;
//   console.log(req.headers.get("key"));

//   if (req.headers.get("key") !== apiKey) return false;
//   return true;
// };
